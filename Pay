app.post("/checkout", async (req, res) => {
    // Create your order

    const payment = await createWiPayPayment({
        amount: 100000.00,
        currency: "JMD",
        return_url: "https://yourstore.com/payment-success",
        cancel_url: "https://yourstore.com/payment-cancel"
    });

    res.redirect(payment.payment_url);
});

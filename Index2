<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Shan Online Store</title>

<style>
*{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family:Arial,sans-serif;
}

body{
    background:#f4f4f4;
}

header{
    background:#0d6efd;
    color:white;
    padding:15px 30px;
    display:flex;
    justify-content:space-between;
    align-items:center;
}

header a{
    color:white;
    text-decoration:none;
    font-weight:bold;
}

.container{
    width:90%;
    max-width:1200px;
    margin:auto;
    padding:20px;
}

.products{
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(250px,1fr));
    gap:20px;
}

.product{
    background:white;
    border-radius:10px;
    overflow:hidden;
    box-shadow:0 2px 10px rgba(0,0,0,.1);
}

.product img{
    width:100%;
    height:200px;
    object-fit:cover;
}

.product-content{
    padding:15px;
}

.product h3{
    margin-bottom:10px;
}

.price{
    color:green;
    font-size:20px;
    margin:10px 0;
}

button,
input[type="submit"]{
    background:#0d6efd;
    color:white;
    border:none;
    padding:10px 15px;
    cursor:pointer;
    border-radius:5px;
}

button:hover,
input[type="submit"]:hover{
    background:#0b5ed7;
}

.remove-button{
    background:#dc3545;
    margin-top:8px;
}

.remove-button:hover{
    background:#c82333;
}

section{
    margin-top:40px;
}

.cart-item{
    display:flex;
    justify-content:space-between;
    align-items:center;
    background:white;
    padding:10px;
    margin-bottom:10px;
    border-radius:8px;
}

.cart-item div{
    max-width:70%;
}

.cart-item small{
    color:#666;
}

.total{
    font-size:22px;
    font-weight:bold;
    margin-top:20px;
}

form{
    background:white;
    padding:20px;
    border-radius:10px;
}

input,textarea,select{
    width:100%;
    padding:10px;
    margin:10px 0;
    border:1px solid #ccc;
    border-radius:5px;
}

.confirmation,
.receipt-card{
    display:none;
    padding:20px;
    margin-top:20px;
    border-radius:10px;
}

.confirmation{
    background:#d4edda;
    color:#155724;
}

.receipt-card{
    background:white;
    color:#333;
    box-shadow:0 2px 10px rgba(0,0,0,.08);
}

.receipt-card button{
    margin-top:15px;
}

@media (max-width: 800px){
    header{
        flex-direction:column;
        align-items:flex-start;
        gap:10px;
    }
}

.product h3{
    margin-bottom:10px;
}

.price{
    color:green;
    font-size:20px;
    margin:10px 0;
}

button{
    background:#0d6efd;
    color:white;
    border:none;
    padding:10px 15px;
    cursor:pointer;
    border-radius:5px;
}

button:hover{
    background:#0b5ed7;
}

section{
    margin-top:40px;
}

.cart-item{
    display:flex;
    justify-content:space-between;
    background:white;
    padding:10px;
    margin-bottom:10px;
}

.total{
    font-size:22px;
    font-weight:bold;
    margin-top:20px;
}

form{
    background:white;
    padding:20px;
    border-radius:10px;
}

input,textarea{
    width:100%;
    padding:10px;
    margin:10px 0;
}

.confirmation{
    display:none;
    background:#d4edda;
    color:#155724;
    padding:20px;
    margin-top:20px;
    border-radius:10px;
}
</style>
</head>
<body>

<header>
<h2>Shan Online Store</h2>
<a href="#cart">🛒 Cart (<span id="cartCount">0</span>)</a>
</header>

<div class="container">

<section>

<h2>Products</h2>

<div class="products" id="productList"></div>

</section>

<section id="cart">

<h2>Shopping Cart</h2>

<div id="cartItems"></div>

<div class="total">
Total: $<span id="total">0.00</span>
</div>

</section>

<section>

<h2>Checkout</h2>

<form id="checkoutForm">

<input
type="text"
placeholder="Full Name"
required
>

<input
type="email"
placeholder="Email"
required
>

<textarea
placeholder="Delivery Address"
required></textarea>

<textarea
placeholder="Notes (Optional)"
></textarea>

<button type="submit">
Place Order
</button>

</form>

<div class="confirmation" id="confirmation">
<h2>✅ Thank You!</h2>
<p>Your order has been placed successfully.</p>
</div>

<div class="receipt-card" id="receipt">
    <h2>Order Receipt</h2>
    <pre id="receiptContent"></pre>
    <button id="emailReceiptButton">Email Receipt</button>
</div>

</section>

</div>

<script>
const products = [
    {
        name: "T-Shirt",
        price: 450.00,
        description: "Cotton, nylon, polyester.",
        image: "https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fombre.com%2Fhpeciai%2F98f4e5298960491082b4c1b3c41edda7%2Feng_pl_Set-of-mens-t-shirts-with-round-neckline-white-black-V2-Z101-128017_1.webp&f=1&nofb=1&ipt=bca65e1ed0d2327bd9c2012535aad34b65c8dc7fcfdb687668d657a49bfe3d1b"
    },
    {
        name: "Sweater",
        price: 600.00,
        description: "Thin or thick styles available.",
        image: "https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fi5.walmartimages.com%2Fseo%2FFartey-Cable-Knitted-Chunky-Sweater-for-Women-Classic-Solid-Color-Crew-Neck-Pullover-Tops-Fall-Winter-Warm-Long-Sleeve-Sweaters_3d949d27-44dc-4a2d-bba1-ac0ab9aba546.31b6ed60630cc16badc7be90667ef865.jpeg&f=1&nofb=1&ipt=833ef06a1721b74ff8486f3d283b650419f9ea571526eb1f13ac56632d1710a1"
    },
    {
        name: "Caps",
        price: 350.00,
        description: "Water-resistant and stylish.",
        image: "https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fst4.depositphotos.com%2F36526276%2F37837%2Fi%2F450%2Fdepositphotos_378374370-stock-photo-realistic-hat-white-baseball-cap.jpg&f=1&nofb=1&ipt=cfe1f7efa32709f2a801448f0925d9fb712a89ba41cd51be71dddd3991aefd17"
    },
    {
        name: "Mugs and Plates",
        price: 350.00,
        description: "Ceramic Mug, Cups, tubular and plates sets.",
        image: "https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fwww.pngmart.com%2Ffiles%2F5%2FTea-Cup-PNG-Clipart.png&f=1&nofb=1&ipt=beebce79d969f4112fae19a94c20e0ba65c5562beccda8c5d0b17dd76d7ead70"
    }
];

let cart = [];

const productList = document.getElementById("productList");
const cartItems = document.getElementById("cartItems");
const total = document.getElementById("total");
const cartCount = document.getElementById("cartCount");
const confirmation = document.getElementById("confirmation");
const receipt = document.getElementById("receipt");
const receiptContent = document.getElementById("receiptContent");
const emailReceiptButton = document.getElementById("emailReceiptButton");

function displayProducts() {
    productList.innerHTML = "";

    products.forEach((product, index) => {
        productList.innerHTML += `
            <div class="product">
                <img src="${product.image}" alt="${product.name}">
                <div class="product-content">
                    <h3>${product.name}</h3>
                    <p>${product.description}</p>
                    <div class="price">$${product.price.toFixed(2)}</div>
                    <button onclick="addToCart(${index})">Add to Cart</button>
                </div>
            </div>
        `;
    });
}

function addToCart(index) {
    cart.push({ ...products[index], cartId: Date.now() + Math.random() });
    updateCart();
}

function removeFromCart(cartIndex) {
    cart.splice(cartIndex, 1);
    updateCart();
}

function updateCart() {
    cartItems.innerHTML = "";
    let sum = 0;

    if (cart.length === 0) {
        cartItems.innerHTML = "<p>Your cart is empty. Add an item to begin.</p>";
    } else {
        cart.forEach((item, index) => {
            sum += item.price;
            cartItems.innerHTML += `
                <div class="cart-item">
                    <div>
                        <strong>${item.name}</strong><br>
                        <small>${item.description}</small>
                    </div>
                    <div>
                        <span>$${item.price.toFixed(2)}</span><br>
                        <button class="remove-button" onclick="removeFromCart(${index})">Remove</button>
                    </div>
                </div>
            `;
        });
    }

    total.textContent = sum.toFixed(2);
    cartCount.textContent = cart.length;
}

function formatReceipt(name, email, address, notes, items, totalAmount) {
    const lines = [
        "Shan Online Store Receipt",
        "-------------------------",
        `Customer: ${name}`,
        `Email: ${email}`,
        `Delivery Address: ${address}`,
        "",
        "Items:",
        ...items.map(item => `- ${item.name}: $${item.price.toFixed(2)}`),
        "",
        `Notes: ${notes || 'None'}`,
        `Total: $${totalAmount.toFixed(2)}`
    ];
    return lines.join("\n");
}

function sendReceiptByEmail(email, subject, body) {
    const mailto = `mailto:${encodeURIComponent(email)}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
    window.location.href = mailto;
}

const checkoutForm = document.getElementById("checkoutForm");
checkoutForm.addEventListener("submit", function(event) {
    event.preventDefault();

    if (cart.length === 0) {
        alert("Your cart is empty.");
        return;
    }

    const name = document.getElementById("customerName").value.trim();
    const email = document.getElementById("customerEmail").value.trim();
    const address = document.getElementById("customerAddress").value.trim();
    const notes = document.getElementById("orderNotes").value.trim();

    const totalAmount = cart.reduce((sum, item) => sum + item.price, 0);
    const receiptText = formatReceipt(name, email, address, notes, cart, totalAmount);

    receiptContent.textContent = receiptText;
    receipt.style.display = "block";
    confirmation.style.display = "block";

    emailReceiptButton.onclick = function() {
        sendReceiptByEmail(email, "Your Shan Online Store Receipt", receiptText);
    };

    cart = [];
    updateCart();
    checkoutForm.reset();
    window.scrollTo({ top: document.body.scrollHeight, behavior: "smooth" });
});

window.addEventListener("DOMContentLoaded", () => {
    displayProducts();
    updateCart();
});
</script>

</body>
</html>
